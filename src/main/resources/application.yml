spring:
  application:
    name: minio-s3-poc
  
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

moraes:
  s3:
    bucketName: my-bucket
    accessKey: admin
    secretKey: '12345678'
    host: http://localhost:9000